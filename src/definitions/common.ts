export enum Currency {
  UAH = "UAH",
  USD = "USD",
}

export enum ExpenseCategory {
  Food = "Food",
  Car = "Car",
  Transport = "Transport",
}

export enum IncomeCategory {
  Salary = "Salary",
  Crypto = "Crypto",
}
